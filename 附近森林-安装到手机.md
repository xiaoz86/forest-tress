# 附近森林 · 安装到手机

> 一片让正在独立做事的人彼此看见的森林

网址：**https://nearby-forest.club**

不用下载、不用应用商店。把它加到手机主屏，以后一点就进。

---

## 先说清楚现在是什么状态

附近森林目前是**网站**，不是已经打包好的 app。加到主屏之后：

| | 现在 |
| --- | --- |
| 主屏图标 | ✅ 正常，是森林的树形图标 |
| 一点直达 | ✅ 不用再输网址 |
| 全屏无地址栏 | ❌ 打开后仍带浏览器地址栏 |
| 断网可用 | ❌ 需要联网 |
| 出现在系统「应用」列表 | ❌ 只是主屏上的一个入口 |

**为什么**：站点还没有配 PWA 清单（`manifest`）和离线缓存（`service worker`），所以浏览器只能把它当成一个带图标的快捷方式。图标能正常显示，是因为站点已经放好了 180×180 的 `apple-touch-icon`。

想要真正的全屏 app 效果，见文末「[要变成真正的 app，还差什么](#五要变成真正的-app还差什么)」。

---

## 一、装到 iPhone（必须用 Safari）

1. 用 **Safari** 打开 `nearby-forest.club`（Chrome、微信内置浏览器都加不了主屏）；
2. 点右下角「**···**」→「**共享**」→ 在分享面板里向下滑 →「**添加到主屏幕**」；
3. 名字可以改短一点，比如「附近森林」，右上角点「**添加**」；
4. 主屏出现森林图标，点开直接进站。

> iOS 26 把 Safari 底栏改成了紧凑布局，分享按钮不再单独显示，收进了右下角的「···」里。旧版 iOS 是底部中间直接有分享按钮。
>
> 如果分享面板里滑到底也没有「添加到主屏幕」，是它被隐藏了：面板最底部点「**编辑操作…**」→ 找到该项 → 点左侧绿色 `+` 加回来。

---

## 二、装到安卓（用 Chrome）

1. 用 **Chrome** 打开 `nearby-forest.club`；
2. 点右上角「**⋮**」→「**添加到主屏幕**」→ 弹窗点「**添加**」；
3. 图标出现在主屏。

**注意**：安卓这边现在看到的是「添加到主屏幕」，**不会**出现「安装应用」——后者需要站点提供 PWA 清单，我们还没有。所以它不会变成系统「应用」列表里的独立条目，打开时也带 Chrome 地址栏。

**浏览器支持**：Chrome、Edge、三星浏览器都能加主屏；微信 / QQ 内置浏览器不行，得先点右上角「**在浏览器中打开**」。小米、华为、OPPO 自带浏览器多数基于 Chromium，找「添加到桌面」之类的字样。

---

## 三、登录

附近森林**不设访问口令**——首页、创造者列表、林间探索这些内容，任何人打开就能看。

需要登录的只有这几件事：编辑自己的节点卡、查看 AI 为你找的同频伙伴、看别人的联系方式、进入已解锁的冥想内容。

**怎么登录**：

1. 页面右上角「**个人中心**」→ 进入登录页；
2. 填**注册时用的邮箱**，点「发送验证码」；
3. 邮箱会收到一封带六位数字的信，把数字填回页面。

验证码 **10 分钟内有效、只能用一次**。登录之后登录态保留 **180 天**，这半年里不用再登。

> 收不到验证码先翻**垃圾邮件**文件夹。QQ 邮箱、Outlook 都有把它判成垃圾邮件的先例。

---

## 四、几个要知道的点

**麦克风**：和 PhilCoach 说话时会用到语音输入，第一次会弹权限请求，允许即可。
误点拒绝后——iOS 去 `设置 → Safari → 麦克风`；安卓去 `设置 → 应用 → Chrome → 权限 → 麦克风`。
注意：因为主屏图标本质上还是浏览器，权限跟着**浏览器**走，不是跟着这个图标走。

**数据在云端，多设备互通**：节点卡、作品、冥想解锁记录、听后感悟全都存在服务器上。手机、平板、电脑登录同一个邮箱看到的是同一份，删掉主屏图标**不会**丢任何数据。
（唯一例外是 PhilCoach 的对话——它默认不保存，说完就散。）

**更新**：站点更新后手机上自动就是新版，不用重装。

**语言**：站点会按访问来源自动判断——中国大陆、港澳台显示简体中文，其他地区显示英文。想手动切，点导航栏右侧的「**EN / 中**」。选择会记住一年。

**流量**：林间探索里的冥想音频、首页的森林视频都不小，用移动数据听整段冥想前留意一下。

---

## 五、要变成真正的 app，还差什么

想达到雅伴那种「全屏、无地址栏、能离线、在系统应用列表里」的效果，需要补这几样：

| 要加的 | 作用 |
| --- | --- |
| `src/app/manifest.ts` | PWA 清单：app 名、独立窗口（`display: standalone`）、主题色、启动地址 |
| 192 / 512 图标（含 maskable） | 安卓 WebAPK 的硬性要求，现在只有 iOS 用的 180×180 |
| `layout.tsx` 补 meta | `appleWebApp`、`themeColor`、`viewport-fit=cover`（适配刘海和底部安全区） |
| `public/sw.js` + 注册 | Service Worker：缓存静态资源、断网兜底；**API 和音频必须排除**，否则会缓存到过期内容和付费音频 |
| `src/app/offline/page.tsx` | 断网时的提示页 |

补完之后：iOS 上打开无地址栏，安卓上 Chrome 会主动弹「安装附近森林」横幅、装出来是真正的 WebAPK。

**工作量不大，主要是图标和 Service Worker 的缓存策略要想清楚**——尤其冥想音频走的是带签名的私有链接（一小时过期），绝对不能进缓存。

---

## 附：现在站点已有的相关配置

核对自线上 `nearby-forest.club` 返回的 HTML：

| 项 | 状态 |
| --- | --- |
| `apple-touch-icon` 180×180 | ✅ 有 —— iOS 主屏图标正常 |
| `icon.svg` / `favicon.ico` | ✅ 有 —— 浏览器标签页图标正常 |
| `viewport` | ✅ 有 —— 移动端布局正常 |
| `rel="manifest"` | ❌ 无 |
| `apple-mobile-web-app-capable` | ❌ 无 |
| `theme-color` | ❌ 无 |
